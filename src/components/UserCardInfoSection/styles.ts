import makeStyles from 'styles';

const useStyles = makeStyles()(
  () => ({
    userCardSection: {
      display: 'flex',
      height: '69px',
      flexDirection: 'column',
      width: '300px',
    }
  })
);
export default useStyles;
